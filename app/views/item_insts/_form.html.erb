<%= form_for(@item_inst) do |f| %>
  <% if @item_inst.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_inst.errors.count, "error") %> prohibited this item_inst from being saved:</h2>

      <ul>
      <% @item_inst.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.number_field :item_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@market_item_vote) do |f| %>
  <% if @market_item_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@market_item_vote.errors.count, "error") %> prohibited this market_item_vote from being saved:</h2>

      <ul>
      <% @market_item_vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.number_field :item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
